/**
 * Single source of truth for the plugin semver shown in `orca.log`, the
 * diagnostics panel snapshot, and activate diagnostics. Keep in lockstep
 * with `package.json` and `orca-plugin.json`.
 *
 * @module version
 * @author Jonathan Marien
 * @date 2026-09-05
 */

export const PLUGIN_VERSION = '0.5.0'
